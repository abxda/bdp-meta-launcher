# Binarios compilados (se distribuyen via GitHub Releases + Hugging Face).
*.exe
*.com
*.com.dbg
*.aarch64.elf
/meta-launcher
/meta-launcher-*

# Toolchain cosmocc (se descarga aparte).
/cosmocc/
cosmocc.zip

# OS junk
.DS_Store
._*
Thumbs.db

# Token HF — nunca en git
.hf_token
*.token
